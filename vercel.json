{
  "buildCommand": "cd artifacts/web && npx vite build --config vite.config.ts",
  "outputDirectory": "artifacts/web/dist/public",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
